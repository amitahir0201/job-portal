docker compose -f docker-compose.prod.yml up

This command will start the backend and frontend services defined in the `docker-compose.prod.yml` file. The backend service will be accessible on port 5000, and the frontend service will be accessible on port 3000. Make sure to have Docker installed and running on your machine before executing this command.